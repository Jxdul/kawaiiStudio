<UserControl x:Class="KawaiiStudio.App.Views.CaptureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F6F2EA">
  <Grid>
    <Image Source="{Binding BackgroundPath, Converter={StaticResource FilePathToImage}}"
           Stretch="UniformToFill"
           Opacity="0.9" />
    <Rectangle Fill="#CCF6F2EA" />
    <Grid Margin="24">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
        <TextBlock Text="Capture" FontSize="28" FontWeight="SemiBold" Margin="16,0,0,0" />
      </DockPanel>

      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <Border Background="#E7DFD6" CornerRadius="12" Margin="0,0,16,0">
          <Grid>
            <Image Source="{Binding LiveViewImage}" Stretch="Uniform" />
            <TextBlock Text="{Binding CountdownText}"
                       FontSize="72"
                       FontWeight="Bold"
                       Foreground="#FFFFFF"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="{Binding IsCountdownActive, Converter={StaticResource BoolToVisibility}}" />
            <TextBlock Text="Live View" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#B8B8B8">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding LiveViewImage}" Value="{x:Null}">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </Grid>
        </Border>

        <StackPanel Grid.Column="1">
          <TextBlock Text="{Binding StatusText}" FontSize="14" Foreground="#B8B8B8" Margin="0,0,0,8" />
          <TextBlock Text="{Binding ProgressText}" FontSize="14" Foreground="#B8B8B8" Margin="0,0,0,12" />
          <Button Content="{Binding CaptureButtonText}" Command="{Binding ContinueCommand}" FontSize="18" Padding="18,10" />
        </StackPanel>
      </Grid>
    </Grid>
  </Grid>
</UserControl>


