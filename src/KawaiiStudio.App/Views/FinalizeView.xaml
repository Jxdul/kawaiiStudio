<UserControl x:Class="KawaiiStudio.App.Views.FinalizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B0B0B">
  <Grid>
    <Image Source="{Binding BackgroundPath, Converter={StaticResource FilePathToImage}}"
           Stretch="UniformToFill"
           Opacity="0.9" />
    <Rectangle Fill="#66000000" />
    <Grid Margin="24">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
        <TextBlock Text="Finalize" FontSize="28" FontWeight="SemiBold" Margin="16,0,0,0" />
      </DockPanel>

      <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Render composite, generate QR, queue print" FontSize="16" Foreground="#B8B8B8" Margin="0,0,0,16" />
        <Button Content="Send to Print" Command="{Binding ContinueCommand}" FontSize="18" Padding="18,10" />
      </StackPanel>
    </Grid>
  </Grid>
</UserControl>
