<UserControl x:Class="KawaiiStudio.App.Views.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F6F2EA">
  <Grid>
    <Rectangle Fill="#CCF6F2EA" />
    <Border Background="#EEE7DD"
            CornerRadius="12"
            Padding="24"
            Width="520"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
      <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Text="{Binding Message}" FontSize="14" Foreground="#B8B8B8" TextWrapping="Wrap" Margin="0,0,0,16" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Content="Enable Test Mode" Command="{Binding EnableTestModeCommand}" Padding="16,8" />
          <Button Content="Close Program" Command="{Binding CloseCommand}" Padding="16,8" Margin="8,0,0,0" />
        </StackPanel>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>


