<UserControl x:Class="KawaiiStudio.App.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F6F2EA">
  <Grid>
    <Image Source="{Binding BackgroundPath, Converter={StaticResource FilePathToImage}}"
           Stretch="UniformToFill"
           Opacity="0.9" />
    <Rectangle Fill="#CCF6F2EA" />
    <Grid Margin="24">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,24">
        <Button Content="Back" 
                Command="{Binding BackCommand}"
                Padding="32,16"
                FontSize="24"
                Background="#E7DFD6"
                Foreground="#333333"
                BorderThickness="0"
                FontWeight="SemiBold"
                Cursor="Hand">
          <Button.Effect>
            <DropShadowEffect Color="#20000000" BlurRadius="6" ShadowDepth="3" />
          </Button.Effect>
          <Button.Style>
            <Style TargetType="Button">
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                      <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
              <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter Property="Background" Value="#DDD5CC" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter Property="Background" Value="#CCC4BB" />
                </Trigger>
              </Style.Triggers>
            </Style>
          </Button.Style>
        </Button>
        <TextBlock Text="Choose a Frame Category" FontSize="40" FontWeight="SemiBold" Margin="24,0,0,0" />
      </DockPanel>

      <Grid Grid.Row="1">
        <TextBlock Text="No categories found for this layout." FontSize="14" Foreground="#B8B8B8" HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock.Style>
            <Style TargetType="TextBlock">
              <Setter Property="Visibility" Value="Visible" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding HasCategories}" Value="True">
                  <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TextBlock.Style>
        </TextBlock>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <Grid HorizontalAlignment="Center">
            <ItemsControl ItemsSource="{Binding Categories}" HorizontalAlignment="Center" MaxWidth="1100">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel ItemWidth="480" ItemHeight="450" HorizontalAlignment="Center" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"
                        Margin="0,0,32,32"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0">
                  <Border Background="#EEE7DD" CornerRadius="18" Padding="24">
                    <Border.Effect>
                      <DropShadowEffect Color="#20000000" BlurRadius="16" ShadowDepth="6" />
                    </Border.Effect>
                    <StackPanel>
                      <Border Background="#E7DFD6" CornerRadius="12" Height="320" Margin="0,0,0,16">
                        <Image Source="{Binding PreviewImagePath, Converter={StaticResource FilePathToImage}}" Stretch="UniformToFill" />
                      </Border>
                      <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding FrameCount, StringFormat={}{0} frames}" FontSize="18" Foreground="#8A8A8A" />
                    </StackPanel>
                  </Border>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          </Grid>
        </ScrollViewer>
      </Grid>
    </Grid>
  </Grid>
</UserControl>


