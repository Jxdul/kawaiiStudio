<UserControl x:Class="KawaiiStudio.App.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F6F2EA">
  <Grid>
    <Image Source="{Binding BackgroundPath, Converter={StaticResource FilePathToImage}}"
           Stretch="UniformToFill"
           Opacity="0.9" />
    <Rectangle Fill="#CCF6F2EA" />
    <Grid Margin="24">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
        <Button Content="Back" Command="{Binding BackCommand}" />
        <TextBlock Text="Choose a Frame Category" FontSize="28" FontWeight="SemiBold" Margin="16,0,0,0" />
      </DockPanel>

      <Grid Grid.Row="1">
        <TextBlock Text="No categories found for this layout." FontSize="14" Foreground="#B8B8B8" HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock.Style>
            <Style TargetType="TextBlock">
              <Setter Property="Visibility" Value="Visible" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding HasCategories}" Value="True">
                  <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TextBlock.Style>
        </TextBlock>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <Grid HorizontalAlignment="Center">
            <ItemsControl ItemsSource="{Binding Categories}" HorizontalAlignment="Center" MaxWidth="1100">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel ItemWidth="320" ItemHeight="300" HorizontalAlignment="Center" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"
                        Margin="0,0,20,20"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0">
                  <Border Background="#EEE7DD" CornerRadius="18" Padding="16">
                    <Border.Effect>
                      <DropShadowEffect Color="#20000000" BlurRadius="16" ShadowDepth="6" />
                    </Border.Effect>
                    <StackPanel>
                      <Border Background="#E7DFD6" CornerRadius="12" Height="180" Margin="0,0,0,12">
                        <Image Source="{Binding PreviewImagePath, Converter={StaticResource FilePathToImage}}" Stretch="UniformToFill" />
                      </Border>
                      <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding FrameCount, StringFormat={}{0} frames}" FontSize="12" Foreground="#8A8A8A" />
                    </StackPanel>
                  </Border>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          </Grid>
        </ScrollViewer>
      </Grid>
    </Grid>
  </Grid>
</UserControl>


