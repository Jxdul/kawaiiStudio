<UserControl x:Class="KawaiiStudio.App.Views.PrintingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B0B0B">
  <Grid>
    <Image Source="{Binding BackgroundPath, Converter={StaticResource FilePathToImage}}"
           Stretch="UniformToFill"
           Opacity="0.9" />
    <Rectangle Fill="#66000000" />
    <Grid Margin="24">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Printing" FontSize="32" FontWeight="SemiBold" Margin="0,0,0,12" />
        <TextBlock Text="Your prints are on the way" FontSize="16" Foreground="#B8B8B8" Margin="0,0,0,16" />
        <Button Content="Continue" Command="{Binding ContinueCommand}" FontSize="18" Padding="18,10" />
      </StackPanel>
    </Grid>
  </Grid>
</UserControl>
